stages:
  - development

development:
  stage: development
  script:
    - ./.scripts/update-vsf-dev
  environment:
    name: development
    url: https://dev.copia.odoogap.com/
  only:
    - dev
  tags:
    - vsf
